/// Hyper is a library that makes it easy to build strings of HTML using Dart.
///
/// It's intentionally kept simple, and produces nothing but strings of HTML.
library hyper;

export 'src/hyper_base.dart';
